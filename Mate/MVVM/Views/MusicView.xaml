<UserControl x:Class="Mate.MVVM.Views.MusicView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             UseLayoutRounding="True" SnapsToDevicePixels="True">
    <UserControl.Resources>
        <Style x:Key="PlaybackButton" TargetType="Button">
            <Setter Property="Width" Value="45" /><Setter Property="Height" Value="45" /><Setter Property="Padding" Value="0" />
            <Setter Property="Background" Value="Transparent" /><Setter Property="BorderThickness" Value="0" /><Setter Property="Cursor" Value="Hand" />
            <Setter Property="FocusVisualStyle" Value="{x:Null}" />
            <Setter Property="Template"><Setter.Value><ControlTemplate TargetType="Button"><Border x:Name="ButtonSurface" Background="{TemplateBinding Background}" CornerRadius="22"><ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" /></Border><ControlTemplate.Triggers><Trigger Property="IsMouseOver" Value="True"><Setter TargetName="ButtonSurface" Property="Background" Value="{DynamicResource Theme.SurfacePressed}" /></Trigger><Trigger Property="IsPressed" Value="True"><Setter TargetName="ButtonSurface" Property="Background" Value="{DynamicResource Theme.Divider}" /></Trigger><Trigger Property="IsEnabled" Value="False"><Setter Property="Opacity" Value="0.28" /></Trigger></ControlTemplate.Triggers></ControlTemplate></Setter.Value></Setter>
        </Style>
        <Style x:Key="MediaSlider" TargetType="Slider">
            <Setter Property="Height" Value="18" />
            <Setter Property="Minimum" Value="0" />
            <Setter Property="Maximum" Value="100" />
            <Setter Property="IsMoveToPointEnabled" Value="True" />
            <Setter Property="FocusVisualStyle" Value="{x:Null}" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Slider">
                        <Track x:Name="PART_Track"
                               Height="18"
                               Orientation="Horizontal">
                            <Track.DecreaseRepeatButton>
                                <RepeatButton Command="{x:Static Slider.DecreaseLarge}"
                                              Focusable="False">
                                    <RepeatButton.Template>
                                        <ControlTemplate TargetType="RepeatButton">
                                            <Border Height="4"
                                                    Background="#E7E7E7"
                                                    CornerRadius="2"
                                                    VerticalAlignment="Center" />
                                        </ControlTemplate>
                                    </RepeatButton.Template>
                                </RepeatButton>
                            </Track.DecreaseRepeatButton>
                            <Track.Thumb>
                                <Thumb Width="12" Height="12" Cursor="Hand">
                                    <Thumb.Template>
                                        <ControlTemplate TargetType="Thumb">
                                            <Ellipse x:Name="ThumbSurface"
                                                     Fill="#E7E7E7"
                                                     Stroke="{DynamicResource Theme.WindowBackground}"
                                                     StrokeThickness="2" />
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter TargetName="ThumbSurface" Property="Fill" Value="#FFFFFF" />
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Thumb.Template>
                                </Thumb>
                            </Track.Thumb>
                            <Track.IncreaseRepeatButton>
                                <RepeatButton Command="{x:Static Slider.IncreaseLarge}"
                                              Focusable="False">
                                    <RepeatButton.Template>
                                        <ControlTemplate TargetType="RepeatButton">
                                            <Border Height="4"
                                                    Background="{DynamicResource Theme.ProgressTrack}"
                                                    CornerRadius="2"
                                                    VerticalAlignment="Center" />
                                        </ControlTemplate>
                                    </RepeatButton.Template>
                                </RepeatButton>
                            </Track.IncreaseRepeatButton>
                        </Track>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.35" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid Margin="42,30,38,30">
        <Grid.RowDefinitions><RowDefinition Height="17" /><RowDefinition Height="*" /></Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="{Binding Source}"
                   Foreground="#898989"
                   FontSize="11"
                   HorizontalAlignment="Right"
                   VerticalAlignment="Center"
                   TextTrimming="CharacterEllipsis"
                   MaxWidth="240" />

        <Grid Grid.Row="1" Margin="0,34,0,0">
            <Grid.ColumnDefinitions><ColumnDefinition Width="104" /><ColumnDefinition Width="*" /><ColumnDefinition Width="160" /></Grid.ColumnDefinitions>

            <Border Width="104" Height="104" Background="{DynamicResource Theme.PlaceholderBackground}" CornerRadius="12" VerticalAlignment="Top">
                <Grid ClipToBounds="True">
                    <Grid.Clip><RectangleGeometry Rect="0,0,104,104" RadiusX="12" RadiusY="12" /></Grid.Clip>
                    <Grid><Path Data="M 0,79 C 17,61 35,59 53,68 C 72,78 79,90 104,72 L 104,104 L 0,104 Z" Fill="{DynamicResource Theme.PlaceholderAccent}" /><Ellipse Width="45" Height="45" Fill="{DynamicResource Theme.BorderStrong}" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0,24,17,0" /></Grid>
                    <Image Source="{Binding Thumbnail}" Stretch="UniformToFill" />
                </Grid>
            </Border>

            <Grid Grid.Column="1" Margin="32,15,20,0" VerticalAlignment="Top">
                <Grid.RowDefinitions><RowDefinition Height="Auto" /><RowDefinition Height="Auto" /><RowDefinition Height="42" /><RowDefinition Height="Auto" /></Grid.RowDefinitions>
                <TextBlock Text="{Binding TrackTitle}" Foreground="#F3F3F3" FontSize="24" FontWeight="SemiBold" TextTrimming="CharacterEllipsis" />
                <TextBlock Grid.Row="1" Text="{Binding Artist}" Foreground="#929292" FontSize="14" Margin="0,7,0,0" TextTrimming="CharacterEllipsis" />
                <Grid Grid.Row="3">
                    <Grid.ColumnDefinitions><ColumnDefinition Width="Auto" /><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
                    <TextBlock Text="{Binding ElapsedTime}" Foreground="#8A8A8A" FontSize="12" VerticalAlignment="Center" Margin="0,0,12,0" />
                    <Slider Grid.Column="1"
                            Style="{StaticResource MediaSlider}"
                            Value="{Binding SeekPercentage, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                            IsEnabled="{Binding CanSeek}"
                            VerticalAlignment="Center" />
                    <TextBlock Grid.Column="2" Text="{Binding DurationTime}" Foreground="#8A8A8A" FontSize="12" VerticalAlignment="Center" Margin="12,0,0,0" />
                </Grid>
            </Grid>

            <Grid Grid.Column="2"
                  Width="156"
                  VerticalAlignment="Center"
                  HorizontalAlignment="Center">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <Button Grid.Column="0"
                        Style="{StaticResource PlaybackButton}"
                        ToolTip="Предыдущий трек"
                        Command="{Binding SkipPreviousCommand}">
                    <Grid Width="24" Height="24">
                        <Path Data="M 6,5 L 6,19 M 18,5 L 8,12 L 18,19 Z"
                              Stroke="#E6E6E6"
                              StrokeThickness="1.8"
                              StrokeStartLineCap="Round"
                              StrokeEndLineCap="Round"
                              StrokeLineJoin="Round"
                              Fill="Transparent" />
                    </Grid>
                </Button>

                <Button Grid.Column="1"
                        Style="{StaticResource PlaybackButton}"
                        ToolTip="{Binding PlayPauseToolTip}"
                        Command="{Binding TogglePlayPauseCommand}">
                    <Grid Width="24" Height="24">
                        <Path Data="M 8,5 L 18,12 L 8,19 Z"
                              Stroke="#F5F5F5"
                              StrokeThickness="1.8"
                              StrokeLineJoin="Round"
                              Fill="Transparent">
                            <Path.Style>
                                <Style TargetType="Path">
                                    <Setter Property="Visibility" Value="Visible" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsPlaying}" Value="True">
                                            <Setter Property="Visibility" Value="Collapsed" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Path.Style>
                        </Path>
                        <Path Data="M 9,5 L 9,19 M 15,5 L 15,19"
                              Stroke="#F5F5F5"
                              StrokeThickness="1.8"
                              StrokeStartLineCap="Round"
                              StrokeEndLineCap="Round">
                            <Path.Style>
                                <Style TargetType="Path">
                                    <Setter Property="Visibility" Value="Collapsed" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsPlaying}" Value="True">
                                            <Setter Property="Visibility" Value="Visible" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Path.Style>
                        </Path>
                    </Grid>
                </Button>

                <Button Grid.Column="2"
                        Style="{StaticResource PlaybackButton}"
                        ToolTip="Следующий трек"
                        Command="{Binding SkipNextCommand}">
                    <Grid Width="24" Height="24">
                        <Path Data="M 18,5 L 18,19 M 6,5 L 16,12 L 6,19 Z"
                              Stroke="#E6E6E6"
                              StrokeThickness="1.8"
                              StrokeStartLineCap="Round"
                              StrokeEndLineCap="Round"
                              StrokeLineJoin="Round"
                              Fill="Transparent" />
                    </Grid>
                </Button>
            </Grid>
        </Grid>
    </Grid>
</UserControl>
