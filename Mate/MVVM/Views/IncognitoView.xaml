<UserControl x:Class="Mate.MVVM.Views.IncognitoView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <StackPanel Margin="42,30,38,30">
        <TextBlock Text="{Binding Title}" Foreground="#A1A1A1" FontSize="17" />
        <TextBlock Text="{Binding Description}" Foreground="#8C8C8C" FontSize="14" Margin="0,12,0,0" />
        <TextBox Margin="0,22,0,0" Height="40" Background="#222222" BorderBrush="#3A3A3A" Foreground="#EAEAEA" Padding="12,9" ToolTip="Введите поисковый запрос" />
    </StackPanel>
</UserControl>
