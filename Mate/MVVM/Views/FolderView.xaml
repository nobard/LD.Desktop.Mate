<UserControl x:Class="Mate.MVVM.Views.FolderView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:viewModels="clr-namespace:Mate.MVVM.ViewModels"
             UseLayoutRounding="True" SnapsToDevicePixels="True">
    <UserControl.Resources>
        <Style x:Key="FileCardButton" TargetType="Button">
            <Setter Property="Width" Value="126" />
            <Setter Property="Height" Value="158" />
            <Setter Property="Margin" Value="0,0,16,0" />
            <Setter Property="Padding" Value="0" />
            <Setter Property="Background" Value="#171717" />
            <Setter Property="BorderBrush" Value="#4A4A4A" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="CardSurface" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="12">
                            <ContentPresenter />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True"><Setter TargetName="CardSurface" Property="Background" Value="#202020" /></Trigger>
                            <DataTrigger Binding="{Binding IsSelected}" Value="True"><Setter TargetName="CardSurface" Property="BorderBrush" Value="#AAAAAA" /><Setter TargetName="CardSurface" Property="BorderThickness" Value="2" /></DataTrigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="FooterButton" TargetType="Button">
            <Setter Property="Height" Value="30" />
            <Setter Property="Padding" Value="13,0" />
            <Setter Property="Margin" Value="8,0,0,0" />
            <Setter Property="Foreground" Value="#C7C7C7" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderBrush" Value="#3B3B3B" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="FooterSurface" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="7"><ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" /></Border>
                        <ControlTemplate.Triggers><Trigger Property="IsMouseOver" Value="True"><Setter TargetName="FooterSurface" Property="Background" Value="#292929" /><Setter Property="Foreground" Value="#F0F0F0" /></Trigger></ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid Margin="42,30,38,23">
        <Grid.RowDefinitions><RowDefinition Height="17" /><RowDefinition Height="*" /><RowDefinition Height="30" /></Grid.RowDefinitions>

        <ItemsControl Grid.Row="1" ItemsSource="{Binding Files}" VerticalAlignment="Top" Margin="0,34,0,0">
            <ItemsControl.ItemsPanel><ItemsPanelTemplate><StackPanel Orientation="Horizontal" /></ItemsPanelTemplate></ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate DataType="{x:Type viewModels:FileCardViewModel}">
                    <Button Style="{StaticResource FileCardButton}"
                            Command="{Binding DataContext.ToggleSelectionCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                            CommandParameter="{Binding}">
                        <Grid Margin="8">
                            <Grid.RowDefinitions><RowDefinition Height="75" /><RowDefinition Height="*" /></Grid.RowDefinitions>
                            <Border Grid.Row="0" Background="#2D2D2D" CornerRadius="7" ClipToBounds="True">
                                <Grid>
                                    <Path Data="M 0,47 C 19,28 38,35 57,47 C 77,59 91,54 110,37 L 110,75 L 0,75 Z" Fill="#3E3E3E" />
                                    <Rectangle Width="30" Height="18" Fill="#555555" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0,12,13,0" />
                                    <Rectangle Width="46" Height="7" Fill="#686868" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="12,13,0,0" />
                                </Grid>
                            </Border>
                            <StackPanel Grid.Row="1" Margin="2,9,2,0">
                                <TextBlock Text="{Binding Name}" Foreground="#E8E8E8" FontSize="12" HorizontalAlignment="Center" TextTrimming="CharacterEllipsis" />
                                <TextBlock Text="{Binding CapturedAt}" Foreground="#929292" FontSize="10" HorizontalAlignment="Center" Margin="0,4,0,0" TextTrimming="CharacterEllipsis" />
                            </StackPanel>
                            <Border Width="17" Height="17" CornerRadius="9" Background="#E6E6E6" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="-3,-3,0,0">
                                <TextBlock Text="✓" Foreground="#1A1A1A" FontSize="11" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center" />
                                <Border.Style><Style TargetType="Border"><Setter Property="Visibility" Value="Collapsed" /><Style.Triggers><DataTrigger Binding="{Binding IsSelected}" Value="True"><Setter Property="Visibility" Value="Visible" /></DataTrigger></Style.Triggers></Style></Border.Style>
                            </Border>
                        </Grid>
                    </Button>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>

        <Grid Grid.Row="2">
            <TextBlock Text="{Binding SelectionSummary}" Foreground="#898989" FontSize="12" VerticalAlignment="Center" />
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Style="{StaticResource FooterButton}" Content="Снять выбор" Command="{Binding ClearSelectionCommand}" />
                <Button Style="{StaticResource FooterButton}" Content="Очистить" Command="{Binding ClearFilesCommand}" />
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
