<UserControl x:Class="Mate.MVVM.Views.TranslatorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             UseLayoutRounding="True" SnapsToDevicePixels="True">
    <UserControl.Resources>
        <Style x:Key="TranslationInput" TargetType="TextBox">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Foreground" Value="#F0F0F0" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="CaretBrush" Value="#EDEDED" />
            <Setter Property="FontSize" Value="28" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Padding" Value="0" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
            <Setter Property="TextWrapping" Value="NoWrap" />
            <Setter Property="AcceptsReturn" Value="False" />
            <Setter Property="VerticalScrollBarVisibility" Value="Hidden" />
        </Style>
        <Style x:Key="ClearButton" TargetType="Button">
            <Setter Property="Width" Value="30" /><Setter Property="Height" Value="30" />
            <Setter Property="Foreground" Value="#898989" /><Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" /><Setter Property="FontSize" Value="20" /><Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template"><Setter.Value><ControlTemplate TargetType="Button"><Border x:Name="ButtonSurface" Background="{TemplateBinding Background}" CornerRadius="15"><ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" /></Border><ControlTemplate.Triggers><Trigger Property="IsMouseOver" Value="True"><Setter TargetName="ButtonSurface" Property="Background" Value="#292929" /><Setter Property="Foreground" Value="#EAEAEA" /></Trigger></ControlTemplate.Triggers></ControlTemplate></Setter.Value></Setter>
        </Style>
    </UserControl.Resources>

    <Grid Margin="42,30,38,30">
        <Grid.RowDefinitions><RowDefinition Height="17" /><RowDefinition Height="*" /></Grid.RowDefinitions>
        <Grid Grid.Row="1" Margin="0,25,0,0">
            <Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="1" /><ColumnDefinition Width="*" /></Grid.ColumnDefinitions>

            <Canvas Grid.Column="0" Margin="0,0,28,0">
                <TextBlock Canvas.Left="0" Canvas.Top="0" Text="{Binding SourceLanguage}" Foreground="#858585" FontSize="11" FontWeight="SemiBold" />
                <TextBox Canvas.Left="0" Canvas.Top="42" Width="222" Height="46" Style="{StaticResource TranslationInput}" Text="{Binding SourceText, UpdateSourceTrigger=PropertyChanged}" />
                <Button Canvas.Left="252" Canvas.Top="39" Style="{StaticResource ClearButton}" Content="×" Command="{Binding ClearCommand}" ToolTip="Очистить" />
            </Canvas>

            <Border Grid.Column="1" Background="#2D2D2D" Margin="0,0,0,8" />

            <Canvas Grid.Column="2" Margin="29,0,0,0">
                <TextBlock Canvas.Left="0" Canvas.Top="0" Text="{Binding TargetLanguage}" Foreground="#858585" FontSize="11" FontWeight="SemiBold" />
                <TextBlock Canvas.Left="0" Canvas.Top="42" Text="{Binding TranslatedText}" Foreground="#DCDCDC" FontSize="28" FontWeight="SemiBold" TextWrapping="Wrap" MaxWidth="270" />
            </Canvas>
        </Grid>
    </Grid>
</UserControl>
