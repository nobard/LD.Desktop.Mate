<UserControl x:Class="Mate.MVVM.Views.SnippetsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:models="clr-namespace:Mate.Models"
             xmlns:viewModels="clr-namespace:Mate.MVVM.ViewModels"
             UseLayoutRounding="True"
             SnapsToDevicePixels="True">
    <UserControl.InputBindings>
        <KeyBinding Key="Escape" Command="{Binding CancelEditorCommand}" />
    </UserControl.InputBindings>

    <UserControl.Resources>
        <Style x:Key="SearchBox" TargetType="TextBox" BasedOn="{StaticResource MateTextBoxBase}">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Foreground" Value="#E8E8E8" />
            <Setter Property="CaretBrush" Value="#E8E8E8" />
            <Setter Property="FontSize" Value="13" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
        </Style>

        <Style x:Key="HeaderButton" TargetType="Button">
            <Setter Property="Height" Value="34" />
            <Setter Property="Padding" Value="13,0" />
            <Setter Property="Foreground" Value="#D5D5D5" />
            <Setter Property="Background" Value="{DynamicResource Theme.ButtonBackground}" />
            <Setter Property="BorderBrush" Value="{DynamicResource Theme.BorderStrong}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Surface" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="8">
                            <ContentPresenter Margin="{TemplateBinding Padding}" HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True"><Setter TargetName="Surface" Property="Background" Value="{DynamicResource Theme.ButtonHover}" /><Setter Property="Foreground" Value="#F2F2F2" /></Trigger>
                            <Trigger Property="IsPressed" Value="True"><Setter TargetName="Surface" Property="Background" Value="{DynamicResource Theme.ButtonPressed}" /></Trigger>
                            <Trigger Property="IsEnabled" Value="False"><Setter Property="Opacity" Value="0.35" /></Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SearchClearButton" TargetType="Button">
            <Setter Property="Width" Value="24" />
            <Setter Property="Height" Value="24" />
            <Setter Property="Foreground" Value="#777777" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="FontSize" Value="15" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="FocusVisualStyle" Value="{x:Null}" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Surface" Background="{TemplateBinding Background}" CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True"><Setter TargetName="Surface" Property="Background" Value="{DynamicResource Theme.SurfacePressed}" /><Setter Property="Foreground" Value="#D8D8D8" /></Trigger>
                            <Trigger Property="IsPressed" Value="True"><Setter TargetName="Surface" Property="Background" Value="{DynamicResource Theme.SelectedBackground}" /></Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <DataTrigger Binding="{Binding HasSearchQuery}" Value="False"><Setter Property="Visibility" Value="Collapsed" /></DataTrigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="SnippetRow" TargetType="Button">
            <Setter Property="Height" Value="30" />
            <Setter Property="Margin" Value="0,0,0,4" />
            <Setter Property="Padding" Value="0" />
            <Setter Property="Background" Value="{DynamicResource Theme.Surface}" />
            <Setter Property="BorderBrush" Value="{DynamicResource Theme.Border}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
            <Setter Property="VerticalContentAlignment" Value="Stretch" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Surface" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Stretch" VerticalAlignment="Stretch" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True"><Setter TargetName="Surface" Property="Background" Value="{DynamicResource Theme.SurfaceHover}" /><Setter TargetName="Surface" Property="BorderBrush" Value="#555555" /></Trigger>
                            <Trigger Property="IsPressed" Value="True"><Setter TargetName="Surface" Property="Background" Value="{DynamicResource Theme.SurfacePressed}" /></Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="DeleteButton" TargetType="Button">
            <Setter Property="Width" Value="24" />
            <Setter Property="Height" Value="24" />
            <Setter Property="Visibility" Value="Collapsed" />
            <Setter Property="Foreground" Value="#686868" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="FontSize" Value="15" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="ToolTip" Value="Удалить" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Surface" Background="{TemplateBinding Background}" CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True"><Setter TargetName="Surface" Property="Background" Value="{DynamicResource Theme.ButtonHover}" /><Setter Property="Foreground" Value="#D8D8D8" /></Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsMouseOver, RelativeSource={RelativeSource AncestorType=Grid}}" Value="True"><Setter Property="Visibility" Value="Visible" /></DataTrigger>
                <DataTrigger Binding="{Binding IsCopied}" Value="True"><Setter Property="Visibility" Value="Collapsed" /></DataTrigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="InputBox" TargetType="TextBox" BasedOn="{StaticResource MateTextBoxBase}">
            <Setter Property="Background" Value="{DynamicResource Theme.SurfaceLow}" />
            <Setter Property="Foreground" Value="#E7E7E7" />
            <Setter Property="CaretBrush" Value="#E7E7E7" />
            <Setter Property="BorderBrush" Value="{DynamicResource Theme.Border}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="10,6" />
            <Setter Property="FontSize" Value="12" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="7">
                            <ScrollViewer x:Name="PART_ContentHost" Focusable="False" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="TypeRadio" TargetType="RadioButton">
            <Setter Property="Width" Value="34" />
            <Setter Property="Height" Value="34" />
            <Setter Property="Padding" Value="0" />
            <Setter Property="Margin" Value="0,0,6,0" />
            <Setter Property="Foreground" Value="#929292" />
            <Setter Property="Background" Value="{DynamicResource Theme.SurfaceLow}" />
            <Setter Property="BorderBrush" Value="{DynamicResource Theme.Border}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Border x:Name="Surface" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="7">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True"><Setter TargetName="Surface" Property="Background" Value="{DynamicResource Theme.SurfaceHover}" /><Setter Property="Foreground" Value="#D2D2D2" /></Trigger>
                            <Trigger Property="IsChecked" Value="True"><Setter TargetName="Surface" Property="Background" Value="{DynamicResource Theme.ButtonPressed}" /><Setter TargetName="Surface" Property="BorderBrush" Value="#686868" /><Setter Property="Foreground" Value="#F0F0F0" /></Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ScrollPageButton" TargetType="RepeatButton">
            <Setter Property="Focusable" Value="False" />
            <Setter Property="Template"><Setter.Value><ControlTemplate TargetType="RepeatButton"><Border Background="Transparent" /></ControlTemplate></Setter.Value></Setter>
        </Style>
        <Style x:Key="ScrollThumb" TargetType="Thumb">
            <Setter Property="MinHeight" Value="28" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Thumb">
                        <Border x:Name="ThumbSurface" Width="8" Background="#969696" CornerRadius="4" HorizontalAlignment="Center" />
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True"><Setter TargetName="ThumbSurface" Property="Background" Value="#B0B0B0" /></Trigger>
                            <Trigger Property="IsDragging" Value="True"><Setter TargetName="ThumbSurface" Property="Background" Value="#C4C4C4" /></Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="ScrollBar">
            <Setter Property="Width" Value="12" />
            <Setter Property="Margin" Value="4,0,0,0" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ScrollBar">
                        <Track x:Name="PART_Track" Orientation="{TemplateBinding Orientation}" IsDirectionReversed="True">
                            <Track.DecreaseRepeatButton><RepeatButton Style="{StaticResource ScrollPageButton}" Command="{x:Static ScrollBar.PageUpCommand}" /></Track.DecreaseRepeatButton>
                            <Track.Thumb><Thumb Style="{StaticResource ScrollThumb}" /></Track.Thumb>
                            <Track.IncreaseRepeatButton><RepeatButton Style="{StaticResource ScrollPageButton}" Command="{x:Static ScrollBar.PageDownCommand}" /></Track.IncreaseRepeatButton>
                        </Track>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers><Trigger Property="IsEnabled" Value="False"><Setter Property="Opacity" Value="0" /></Trigger></Style.Triggers>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="56" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="31,20,24,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <Border Background="{DynamicResource Theme.SearchBackground}" BorderBrush="{DynamicResource Theme.Border}" BorderThickness="1" CornerRadius="8">
                <Grid Margin="11,0">
                    <Grid.ColumnDefinitions><ColumnDefinition Width="21" /><ColumnDefinition Width="*" /><ColumnDefinition Width="26" /></Grid.ColumnDefinitions>
                    <Viewbox Width="15" Height="15" VerticalAlignment="Center"><Path Data="M 7,2 A 5,5 0 1 0 7,12 A 5,5 0 1 0 7,2 M 11,11 L 16,16" Stroke="#858585" StrokeThickness="1.8" StrokeStartLineCap="Round" StrokeEndLineCap="Round" Fill="Transparent" /></Viewbox>
                    <TextBox Grid.Column="1" Style="{StaticResource SearchBox}" Text="{Binding SearchQuery, UpdateSourceTrigger=PropertyChanged}" Margin="4,0,0,0" ToolTip="Поиск по заготовкам" />
                    <Button Grid.Column="2" Style="{StaticResource SearchClearButton}" Content="×" Command="{Binding ClearSearchCommand}" ToolTip="Очистить поиск" />
                </Grid>
            </Border>

            <Button Grid.Column="1" Style="{StaticResource HeaderButton}" Width="80" Height="36" Content="Добавить" Command="{Binding OpenEditorCommand}" Margin="10,0,0,0" VerticalAlignment="Center" />
        </Grid>

        <ScrollViewer Grid.Row="1" Margin="31,11,0,19" VerticalScrollBarVisibility="Visible" HorizontalScrollBarVisibility="Disabled">
            <ItemsControl ItemsSource="{Binding FilteredItems}" Margin="0,0,8,0">
                <ItemsControl.ItemContainerStyle><Style TargetType="ContentPresenter"><Setter Property="HorizontalAlignment" Value="Stretch" /></Style></ItemsControl.ItemContainerStyle>
                <ItemsControl.ItemsPanel><ItemsPanelTemplate><StackPanel Orientation="Vertical" /></ItemsPanelTemplate></ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate DataType="{x:Type viewModels:SnippetItemViewModel}">
                        <Grid>
                            <Button Style="{StaticResource SnippetRow}" Command="{Binding DataContext.CopyItemCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}">
                                <Grid Margin="10,4,65,4">
                                    <Grid.ColumnDefinitions><ColumnDefinition Width="29" /><ColumnDefinition Width="Auto" /><ColumnDefinition Width="*" /></Grid.ColumnDefinitions>
                                    <TextBlock Text="{Binding Icon}" FontFamily="{Binding IconFontFamily}" Foreground="#A0A0A0" FontSize="14" FontWeight="SemiBold" HorizontalAlignment="Center" VerticalAlignment="Center" />
                                    <TextBlock Grid.Column="1" Text="{Binding CommentDisplay}" Foreground="#E0E0E0" FontSize="12" FontWeight="Normal" LineHeight="17" VerticalAlignment="Center" Margin="2,0,14,0" MaxWidth="150" TextTrimming="CharacterEllipsis" />
                                    <TextBlock Grid.Column="2" Text="{Binding Value}" Foreground="#929292" FontSize="12" LineHeight="17" VerticalAlignment="Center" TextTrimming="CharacterEllipsis" />
                                </Grid>
                            </Button>

                            <Button Style="{StaticResource DeleteButton}" ToolTip="Редактировать" Command="{Binding DataContext.EditItemCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,35,4" Panel.ZIndex="2">
                                <Viewbox Width="11" Height="11">
                                    <Canvas Width="16" Height="16">
                                        <Path Data="M 3.5,12.5 L 4.2,9.6 L 10.5,3.3 C 11.2,2.6 12.2,2.6 12.9,3.3 C 13.6,4 13.6,5 12.9,5.7 L 6.6,12 L 3.5,12.5 Z M 9.6,4.2 L 12,6.6"
                                              Stroke="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}"
                                              StrokeThickness="1.45"
                                              StrokeStartLineCap="Round"
                                              StrokeEndLineCap="Round"
                                              StrokeLineJoin="Round"
                                              Fill="Transparent" />
                                    </Canvas>
                                </Viewbox>
                            </Button>

                            <Button Style="{StaticResource DeleteButton}" Command="{Binding DataContext.DeleteItemCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,7,4" Panel.ZIndex="2">
                                <Viewbox Width="10" Height="10">
                                    <Canvas Width="16" Height="16">
                                        <Path Data="M 4,4 L 12,12 M 12,4 L 4,12"
                                              Stroke="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}"
                                              StrokeThickness="1.6"
                                              StrokeStartLineCap="Round"
                                              StrokeEndLineCap="Round" />
                                    </Canvas>
                                </Viewbox>
                            </Button>

                            <Border Background="#E4E4E4" BorderBrush="#F2F2F2" BorderThickness="1" CornerRadius="6" Padding="8,3" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,7,4" Panel.ZIndex="3">
                                <Border.Style><Style TargetType="Border"><Setter Property="Visibility" Value="Collapsed" /><Style.Triggers><DataTrigger Binding="{Binding IsCopied}" Value="True"><Setter Property="Visibility" Value="Visible" /></DataTrigger></Style.Triggers></Style></Border.Style>
                                <TextBlock Text="Скопировано" Foreground="#181818" FontSize="9" FontWeight="SemiBold" />
                            </Border>
                        </Grid>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <Border Grid.Row="1" Margin="31,11,24,19" BorderBrush="{DynamicResource Theme.Border}" BorderThickness="1" CornerRadius="11" IsHitTestVisible="False">
            <Border.Style><Style TargetType="Border"><Setter Property="Visibility" Value="Collapsed" /><Style.Triggers><DataTrigger Binding="{Binding IsEmpty}" Value="True"><Setter Property="Visibility" Value="Visible" /></DataTrigger></Style.Triggers></Style></Border.Style>
            <Grid Height="58" HorizontalAlignment="Center" VerticalAlignment="Center">
                <Grid.RowDefinitions><RowDefinition Height="32" /><RowDefinition Height="26" /></Grid.RowDefinitions>
                <TextBlock Text="@" Foreground="#707070" FontSize="25" FontWeight="SemiBold" HorizontalAlignment="Center" VerticalAlignment="Center" />
                <TextBlock Grid.Row="1" Text="{Binding EmptyMessage}" Foreground="#888888" FontSize="12" LineHeight="17" LineStackingStrategy="BlockLineHeight" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,9,0,0" />
            </Grid>
        </Border>

        <Grid Grid.RowSpan="2" Panel.ZIndex="20">
            <Grid.Style><Style TargetType="Grid"><Setter Property="Visibility" Value="Collapsed" /><Style.Triggers><DataTrigger Binding="{Binding IsEditorOpen}" Value="True"><Setter Property="Visibility" Value="Visible" /></DataTrigger></Style.Triggers></Style></Grid.Style>
            <Border Background="{DynamicResource Theme.EditorBackground}" Padding="31,20,24,19" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="38" />
                        <RowDefinition Height="54" />
                        <RowDefinition Height="73" />
                        <RowDefinition Height="18" />
                    </Grid.RowDefinitions>

                    <Grid>
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                            <RadioButton Style="{StaticResource TypeRadio}" GroupName="SnippetType" IsChecked="{Binding IsTextTypeSelected, Mode=OneWay}" ToolTip="Текст" Command="{Binding SelectTypeCommand}" CommandParameter="{x:Static models:SnippetType.Text}">
                                <TextBlock Text="T" FontFamily="Segoe UI" FontSize="15" FontWeight="SemiBold" />
                            </RadioButton>
                            <RadioButton Style="{StaticResource TypeRadio}" GroupName="SnippetType" IsChecked="{Binding IsLinkTypeSelected, Mode=OneWay}" ToolTip="Ссылка" Command="{Binding SelectTypeCommand}" CommandParameter="{x:Static models:SnippetType.Link}">
                                <TextBlock Text="&#xE71B;" FontFamily="Segoe MDL2 Assets" FontSize="15" />
                            </RadioButton>
                            <RadioButton Style="{StaticResource TypeRadio}" GroupName="SnippetType" IsChecked="{Binding IsEmailTypeSelected, Mode=OneWay}" ToolTip="Почта или пользователь" Command="{Binding SelectTypeCommand}" CommandParameter="{x:Static models:SnippetType.Email}">
                                <TextBlock Text="@" FontFamily="Segoe UI" FontSize="16" FontWeight="SemiBold" />
                            </RadioButton>
                            <RadioButton Style="{StaticResource TypeRadio}" GroupName="SnippetType" IsChecked="{Binding IsPhoneTypeSelected, Mode=OneWay}" ToolTip="Телефон" Command="{Binding SelectTypeCommand}" CommandParameter="{x:Static models:SnippetType.Phone}">
                                <TextBlock Text="&#xE717;" FontFamily="Segoe MDL2 Assets" FontSize="15" />
                            </RadioButton>
                        </StackPanel>

                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                            <Button Style="{StaticResource HeaderButton}" Height="34" Content="Сохранить" Command="{Binding AddItemCommand}" Margin="0,0,8,0" />
                            <Button Style="{StaticResource HeaderButton}" Width="34" Padding="0" Content="×" FontSize="17" ToolTip="Закрыть" Command="{Binding CancelEditorCommand}" Background="Transparent" BorderBrush="{DynamicResource Theme.Border}" />
                        </StackPanel>
                    </Grid>

                    <StackPanel Grid.Row="1">
                        <TextBlock Text="Комментарий · необязательно" Foreground="#777777" FontSize="9" Margin="0,0,0,5" />
                        <TextBox Style="{StaticResource InputBox}" Height="36" Text="{Binding NewComment, UpdateSourceTrigger=PropertyChanged}" />
                    </StackPanel>

                    <StackPanel Grid.Row="2">
                        <TextBlock Text="Текст заготовки" Foreground="#777777" FontSize="9" Margin="0,0,0,5" />
                        <TextBox Style="{StaticResource InputBox}" Height="54" Text="{Binding NewValue, UpdateSourceTrigger=PropertyChanged}" AcceptsReturn="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto" />
                    </StackPanel>

                    <TextBlock Grid.Row="3" Text="{Binding FormError}" Foreground="#B79C9C" FontSize="9" VerticalAlignment="Center" />
                </Grid>
            </Border>
        </Grid>
    </Grid>
</UserControl>
